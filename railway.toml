[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
buildArgs = { COMMIT_SHA = "$RAILWAY_GIT_COMMIT_SHA" }

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
